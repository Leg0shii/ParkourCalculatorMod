# Next session bootstrap: rung 5.375 Lever 2, gate-flip cell-set MILP

## EXECUTION RECORD 2026-07-10 (session cs0710a-j; read before continuing)

Lever 2 BUILT AND RUN. All numbers below are trust-gated (fresh tags in artifact filenames, applied: lines grepped, mtimes checked) and byte-exact verified (worst wall delta 0.0, zero fidelity halts).

- Step 1 harness: CellSetDump.dump (PKC_CELLSET_DUMP=1, _TAG, _SPAN). Enumerates ALL joint cells (gate-flipping INCLUDED) at +-64 sin buckets around the ILS point, bases 0/+-360/+-720; per cell exact float-chain constants (cx,cz movement cast; bx,bz boost cast on t0/13/25/38); bit-exact reconstruction self-check vs ExactJumpModel.forward PASSED (mismatches=0). Artifact: tools/miqcp/rung5375-cellset-span64-cs0710a.json (13838 cells, 130-708/tick).
- Step 2 solver: tools/miqcp/cellset_milp.py (SCIP, free 3-state gates on the |v|<0.1 expand-2 band, 41 X + 14 Z gate ticks, feastol 1e-9, z-lo tighten flag). Python base recompute is byte-identical to the dump (maxAbsDiff 0.0). Model 14478 vars / 14003 binaries.
- R1 LEGAL (tag cs0710c, 3600s): FEASIBLE. THE GATE-FLIP DIRECTION OPENS THE LEGAL PROBLEM (the non-flipping move-table legal MILP was infeasible at the same span). Incumbent legal shortfall 9.683482972e-5 in-model, k=48 moved cells, drift 0.0; dual bound 212.6999059676 = certified in-window legal ceiling bracket [9.40e-5, 9.68e-5] shortfall. The dual bound also certifies PRIMARY dead in-window (pad rhs 212.6999999881 unreachable even legally-relaxed).
- Verification (cs0710f): byte-exact LEGAL at shortfall 9.683582971e-5, viol<=0 on all 19 hard walls, BEATS community 2.74e-4 by 2.83x. legalIls polish seeded at the point (cs0710g): 0 accepts in 4776 evals, the point is 1-opt optimal at span +-24 bases 0/+-360.
- DELIVERED (cs0710h): ATTEMPT_5.375bm_legal.json (game folder) + captures/razor-rung-legal-attempt.json, locked RAW rows, start (214.848020680412200, 0.341922533761863), fresh-reparse PASS (dShortfall 0.0). CAVEAT: maxGameFacing 1437.786 deg (+-1440-class wrap cells, deeper than the 718-deg ILS-attempt precedent); USER IN-TOOL CONFIRMATION PENDING, no record language until then.
- R2 minimax tightened (cs0710d, 3600s): incumbent -1.165021962e-5 (verified cs0710i), dual bound -1.138460640e-5.
- R3 minimax untightened (cs0710e2, 3600s): incumbent -1.152396763e-5 (verified cs0710j, persisted tools/miqcp/rung5375-cellset-point-cs0710e2.json), dual bound -1.138549159e-5. CONTINUITY vs move-table -1.224666605e-5: OK (+7.2e-7 above target, model contains the old neighborhood).
- Consequences: (1) NEW CLOSEST APPROACH 1.152396763e-5 (beats the ILS 1.2246666e-5 floor by 7.2e-7; gate flips break the 5-wall pinch optimality). (2) PRIMARY viol<=0 CERTIFIED DEAD at span +-64 EVEN WITH FREE GATES: max min-slack <= -1.1385e-5 (both domains, feastol 1e-9 caveat). The old certified negatives were conditional on the warm gate pattern; this closes the gate-flip loophole in-window. (3) SECONDARY realized on the base structure as designed.
- Open: R1/R2/R3 all hit timelimit (gaps 0.003%/2.3%/1.2%); bounds stand, exact optimality does not.
- Suite gate green after harness addition (full :core:test --rerun --no-daemon, 2m19s).

WRAP-DEPTH TIER (user ruling 2026-07-10: attempts should prefer |gf| <= 720; the 1437.8-deg attempt confirmed real in-tool but let slide once): cellset_milp.py gained --max-gf (cells filtered by |rep gf|, base cell must survive) and --solver scip|gurobi|copt (gurobi/copt backends built and smoke-tested to their license walls; licenses pending). Capped legal at +-2 deg on the 77k dump (cs0710p) was INCONCLUSIVE (cold on the big model, bound vacuous 212.95, incumbent 6.37e-4). Capped legal on the span-64 dump (cs0710q, 13647 cells): shortfall 9.704655230e-5 in-model, verified byte-exact 9.704755229e-5 (cs0710r), dual bound 212.6999060 = capped ceiling bracket [9.40e-5, 9.70e-5]. THE DEEP-WRAP CELL WAS WORTH ONLY 2.1e-7: the record stands on clean angles. DELIVERED (cs0710t): ATTEMPT_5.375bm_legal_wrap720.json + captures/razor-rung-legal-attempt-wrap720.json, maxGf 717.649, cap-guarded delivery (PKC_CELLSET_MAXGF_CAP), fresh-reparse PASS; user in-tool confirm pending.

FULL-CIRCLE ARM (the last live question, attempted without licenses): tools/miqcp/fullcircle_milp.py = arc-disjunction MILP (K arcs/tick, per-arc realizable norm caps from normcell-regions base set <= +-720, chord inner cut big-M, global polygon at binary-linked radius r[t], gates on ALL ticks, feascenter/ceiling objectives, warm fix-solve-unfix). V1 (K=72, tube +-30 validation, fc0710a): SOUND BUT NON-CONVERGENT: dual bound stuck at 0.125 for 1800s because the fractional-arc root LP is a controllable-thrust disc (conv hull of the annulus = disc); only deep arc branching restores physics. Do NOT rerun K=72 full-circle on SCIP expecting a bound. V2 (K=12 coarse arcs + arc branch priority 1000, fc0710b): SAME 0.125 root bound after 1800s, warm Phase A found no solution in its 300s budget. VERDICT: PARKED on SCIP; the 58k polygon rows make node LPs slow and the disc root admits t=0.125 until deep arc fixing. The in-tube reference remains COPT-certified (-3.72e-6), so nothing is lost; the FULL-CIRCLE question stays OPEN pending (a) licensed COPT/Gurobi spatial B&B on the capped-annulus MIQCP full-circle, (b) Gurobi on this arc-MILP, or (c) a hierarchical/disaggregated reformulation (research-grade). Both licenses requested by the user 2026-07-10 (~2 business days).

WIDEN ARM RUN (same session, continued): span 365 (+-2 deg) dump cs0710k (77884 cells, recon bit-exact). Warm-start remap machinery added to cellset_milp.py (--warm-src-dump/--warm-sol, exact cell-constant matching self-verified to 1e-15, fix-solve-unfix injection because SCIP completesol is unreliable at 78k binaries). R2-widen (cs0710l, minimax tightened, 3600s): incumbent UNCHANGED -1.152396769e-5, dual bound -1.130991861e-5: PRIMARY CERTIFIED DEAD AT +-2 DEG WITH FREE GATES (verified cs0710n). R1-widen (cs0710m, legal, 3600s): incumbent UNCHANGED shortfall 9.683482978e-5, dual bound 212.6999065907 = certified legal-ceiling bracket [9.34e-5, 9.68e-5] at +-2 deg (verified cs0710o): the delivered point sits within 3.5e-6 of the in-window optimum. Gate-flip direction now CLOSED at both spans for PRIMARY; SECONDARY realized and near-ceiling. Caveats for dossier use: SCIP-only certificates (COPT unlicensed cap excludes 78k binaries; two-solver standard unmet without Gurobi eval, user sign-off required), timelimit bounds (not exact optima), feastol 1e-9, window/base-set/gate-band scope conditions as in the mission doc.

Written 2026-07-10 ~01:45 at the end of the overnight execution session. Read this first, then the EXECUTION RECORD header of mission-5375-derived-2026-07-09.md, then razor-campaign-2026-07-09-handoff.md for the standing traps. Everything below is certified with artifacts on disk unless marked otherwise.

## 1. Binding rules (unchanged)

Records are LEGAL-METRIC only; community 2.74e-4 stands. Nothing is claimed until it replays clean in the user's live tool; headless byte-exact is necessary, not sufficient. Locked RAW rows only, no wrapAll in verify paths, display tick = internal + 1, rung patch via RazorFixtures.applyRung5375Patch (count-asserted 3), never edit ExactJumpModel/McSineTable/Constants. User rulings: prepend run-up is legal if the start stays inside the original tick-0 box; mid-chain insertion arms skipped by directive.

ANTI-STALE PROTOCOL (mandatory, verbatim from the campaign after 10+ multi-hour stale-run losses):
1. Mint a FRESH run tag for every harness/solver invocation (e.g. cs0710a, incrementing); never reuse a prior session's tag (sv0709a, nc0709a, li0710a are spent). The tag goes INTO the artifact filename so a stale file cannot masquerade as this run's output.
2. Before reading ANY number from a run: grep the applied:/parameter-echo line in the fresh test XML or log, match the tag, and check the artifact mtime against the invocation time.
3. Env-gated gradle runs ALWAYS use --rerun --no-daemon (the daemon does not propagate fresh env vars to forked test JVMs, and Gradle marks tests UP-TO-DATE on env-only changes; both silently produce stale results).
4. Long runs are direct background commands in the main session, never subagent-supervised (8+ silent subagent deaths on record). Watchdog armed from minute one: no artifact/log growth for 20 min = verify the process CPU before acting; COPT is legitimately log-silent up to ~25 min in deep B&B.
5. After any kill: sweep for orphaned python build_model/cellset processes before relaunching (a surviving orphan overwrites the new run's output file).

## 2. Certified state (2026-07-09/10 session, all COPT gap 1e-8 optimal unless noted)

- PRIMARY (viol <= 0) is DEAD IN-TUBE (+-30 deg, +-60 on inserted ticks) for base, pre-k1, pre-k2, ref-k0, preref-k1: per-tick-capped annulus feascenter t* = -3.722e-6 / -3.773e-6 / -3.715e-6 / -3.535e-6 / -3.484e-6. The capped model is a relaxation (window-max caps), so these negatives bind byte-exact reality inside the tube.
- Structure levers are exhausted: prepend saturates at k=1 (circle t* -1.431e-5 vs -1.428e-5 at k=2); reface (mirror strafe on diagonal t14-24) is redundant with prepend, not additive; mid-chain run-tick insertions are ~0.09 below pad (friction x0.546 drains carried speed).
- Binding structure: a 7-wall vise, in base indexing X@49lo (pad), Z@12lo (raised rung edge), X@37lo, X@24hi, Z@48, Z@41, X@42, all pinched at t* at the feascenter optimum.
- SECONDARY (legal < 2.74e-4) in-model ceilings, capped annulus, certified 25-129s each: base shortfall 3.101e-5 (ceiling 212.6999689814), ref-k0 2.935e-5, preref-k1 2.909e-5. All ~9x inside the community record. Base is within 6% of best: realize on the BASE structure (all existing machinery unchanged).
- Lever 1 RUN AND CLOSED: legal MOVE-MILP (move_milp.py mode=legal) INFEASIBLE over the whole non-gate-flipping move table at span +-16 AND +-64 (8642 moves, cap 500). The X-vise in numbers: X@37lo needs tx >= -0.2063039, X@42 needs tx <= -0.2063284, overlap deficit 2.45e-5. Padless minimax (mode=padless): EMPTY SELECTION optimal at -1.2247e-5 at BOTH spans, i.e. the ILS point is a balanced 5-wall pinch and a fortress local optimum even with the pad wall deleted.
- Smooth-seeded realization collapses (verdict reproduction #3): legalIls from the capped legal ceiling incumbent reached byte-exact legality but at shortfall 1.38e-2. Do not re-try smooth-seeded descent.
- Norm-cell ground truth: NormCellProbe (nc0709a) replicated all 39 prior scan lines exactly; signed region table validated at 13 wrap bases to +-2880: (0,90) norm<1 only, (90,180) norm>1 only, (180,270) clean, (270,360) both; envelope law approximate per-degree (excess to ~1.7e-6); per-tick two-sided caps in core/build/reports/normcell-caps-nc0709a.json, per-degree regions in normcell-regions-nc0709a.json.

## 3. The one unexplored direction

The span-64 move dump DISCARDED 4803 gate-flipping cells (additive tables require a fixed gate pattern). Every negative above is conditional on the warm gate pattern. Lever 2's exact-cell-set MILP with 3-state gate disjunctions searches over gate patterns natively; it is designed in mission-5375 section Lever 2 and never built. Tonight's results re-aim it: the LEGAL objective (R1) is the primary target, feasibility (R2) second.

## 4. Build spec (start narrow, widen on signal)

Step 1, cell dump (Java, new env-gated test-source harness, pattern on MiqcpClose.moveDump): per tick of the base rung spec, enumerate ALL joint cells within +-64 sin buckets of the ILS point gf (tools/miqcp/rung5375-ils-point.json), bases {0,+-360,+-720}, INCLUDING gate-flipping cells (no pattern filter). Per cell emit the exact per-tick step constants the MILP recurrence needs, byte-consistent with ExactJumpModel: the effective movement components (mMag * rotated LUT pair) and, on legacy boost ticks (t0/13/25/38), the boost components from the boost cast chain. Emit per tick: f4, mMag, contact/slip/jump/sprint flags, and the cell list (id, gf, cx, cz, bx, bz, norm). Deliverable: tools/miqcp/rung5375-cellset-span64-<tag>.json plus a count line for the applied: grep.

Step 2, cell-set MILP (Python, new tools/miqcp/cellset_milp.py; SCIP primary since COPT unlicensed caps at 2000 vars): per tick, binaries y[t][c] with sum_c y[t][c] == 1; velocity recurrence exactly as build_model.py solve_scip but with movement add = sum_c y[t][c]*(cx,cz) (+ boost terms on boost ticks); gates as the existing 3-state indicator disjunction on EVERY contact-adjacent tick in the |v|<0.1 band, expand 2 (gate binaries FREE, this is the point); tx/tz continuous over the authored domain with z-lo tightened +0.0625 to the rung edge (see legalIls). Objectives, in run order: (R1) maximize pad eval E(X@49lo), all other walls slack >= 0; (R2) feasibility / max min-slack with the pad included; (R3) minimax as a continuity check against the span-64 move-table result (-1.2247e-5; a large disagreement means a model bug, investigate before trusting anything).
Warm start: the ILS point's cells + its gate pattern.
Scale estimate: ~800-1500 cells/tick x 49 ticks = 40-75k binaries; SCIP handles it but expect minutes-to-hours; time-limit 3600 first, watchdog armed.

Step 3, verification (the arbiter): any R1/R2 incumbent -> reconstruct gf from selected cell reps -> ExactJumpModel.forward -> translated legal scoring via SnapRepairPolish.bestTranslationObj against the pad-less compiled spec (the legalIls code shows the exact pattern including the z-lo tightening) -> report model-vs-exact per-wall deltas (double-recurrence spot check, expect ~1e-13, halt above 1e-9). A model-feasible selection is a CANDIDATE, never a claim.

Step 4, outcomes:
- Verified legal shortfall < 2.74e-4: polish with legalIls seeded AT that point (small budget), then delivery: locked RAW rows, fresh-process reparse, ATTEMPT_5.375bm_legal.json to the game folder, USER in-tool confirmation before any record language.
- R2 feasible selection verified viol <= 0: that is the PRIMARY; same delivery bar, SOLVED naming.
- Both negative at span +-64: widen the window (+-2 deg arm), and if still negative the dossier now has the gate-flip direction closed too; assemble per mission section Dossier with the variants appendix (most PRIMARY-side evidence is already certified).

## 5. Tooling map (all new pieces from tonight, ready to reuse)

- StructureVariantDump (PKC_SVAR=1, PKC_SVAR_SPEC="name:k,...", PKC_SVAR_TAG): variant fixture authoring, arms none/pre/t12/land25/pre38/ref/preref, trust-gated; dumps to core/build/reports/miqcp-dump-svar-*.json.
- NormCellProbe (PKC_NCP=1, PKC_NCP_TAG): region table + caps; hard replication gate against miqcp-normils-rung5375.txt.
- build_model.py new flags: --goal-wall (loud failure if absent), --tube-wide-ticks/--tube-wide-deg, --v0-eps, --annulus-caps <regions json> (per-tick realizable caps), --feascenter (goal hard + max uniform slack; the cheap decisive probe, use it instead of 1e-8 ceilings on pad-straddling questions).
- move_milp.py modes: minimax | legal | padless (args: pool timeS maxK mode goalWall); table span-16 preserved as rung5375-move-table-span16.json, current table is span-64.
- MiqcpClose.legalIls (PKC_MIQCP_LEGALILS=1, _S, _SPAN, _SRC json with incumbent_yaws_deg, _TAG): legal-objective 1-opt lattice descent + the legal translated-scoring pattern (bestTranslationObj on pad-less compiled, z-lo +0.0625).
- Results artifacts: tools/miqcp/results-svar-*-{capfeas,capceil,feas,annfeas}.json + logs, rung5375-legal-point-li0710a.json, move-milp-legal-*.log.

## 6. Operational notes from tonight

- COPT can go log-silent for 25+ minutes in deep B&B at ~1 thread while healthy: verify process CPU before calling a stall; watchdog threshold 20 min, and check liveness with ps -W in git bash (nested PowerShell liveness probes break on quoting).
- Feascenter certificates on these models run 90-300s where the equivalent ceiling grind burned 100 minutes without an answer. Frame every decisive question as feasibility/feascenter if possible.
- The suite gate is green as of session end (full :core:test, exit 0, new harnesses inert without env flags).
